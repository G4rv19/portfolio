{
  "builds": [
    {
      "src": "/app/(.*)",
      "use": "vercel-python@3.8"
    }
  ],
  "routes": [
    { "src": "/app/templates/(.*)", "dest": "/app/templates/$1" },
    { "src": "/(.*)", "dest": "/app/vercel_wrapper.py" }
  ]
}
