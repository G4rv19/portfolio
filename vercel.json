{
  "builds": [
    {
      "src": "/(.*)",
      "use": "vercel-python@3.8"
    }
  ],
  "routes": [
    { "src": "/templates/(.*)", "dest": "/templates/$1" },
    { "src": "/(.*)", "dest": "/vercel_wrapper.py" }
  ]
}
